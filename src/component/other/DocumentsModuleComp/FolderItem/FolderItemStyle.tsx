import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: { width: '48%', backgroundColor: '#E0E0E0', borderRadius: 8, padding: 16, alignItems: 'center', marginBottom: 8 },
    name: { marginTop: 8, color: '#333', fontWeight: '600' },
  });
  export default styles;